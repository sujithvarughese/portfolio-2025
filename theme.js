import { createTheme } from '@mantine/core'

const theme = createTheme({
  fontFamily: 'Montserrat, Trebuchet, Verdana, sans-serif',
  fontFamilyMonospace: 'Montserrat, Trebuchet, Monaco, Courier, monospace',
  headings: { fontFamily: 'Montserrat, Trebuchet, Greycliff CF, sans-serif' },
})

export default theme